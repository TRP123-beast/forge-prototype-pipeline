# Context files

These are static text blocks that get inserted into the master generation prompt at runtime. They change rarely and should be versioned through normal git history (no _v1 suffix needed).

- `trp_bootstrap.md` — TRP product context. Owned by Dev A's Task 1.1. Replace this file with the real one when it's ready.
- `react_template.md` — React/Tailwind conventions Claude should follow. Owned by you (Dev B). Edit freely as you learn what works.
